# Gemini API 키 — 값은 직접 넣으세요. 절대 커밋하지 마세요.
GEMINI_API_KEY=

# 사용할 모델 (비우면 기본값 사용)
GEMINI_MODEL=gemini-3.8-flash
GEMINI_FALLBACK_MODEL=gemini-3.6-flash

# auto | gemini | mock
#   auto : 키가 있으면 gemini, 없으면 mock (기본)
#   mock : 키가 있어도 목업 사용 (데모·개발용)
PLAN_SOURCE=auto
